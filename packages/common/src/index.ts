import * as path from 'path';

const pathVars = {
  front: path.join(__dirname, '..', '..', 'frontend', 'public'),
  back: path.join(__dirname, '..', '..', 'backend', 'dist'),
};

export default pathVars;
